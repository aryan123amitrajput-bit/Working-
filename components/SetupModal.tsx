
// This component is deprecated and replaced by the App-level blocking screen.
// Kept empty to prevent import errors if lazily loaded elsewhere.
import React from 'react';

const SetupModal = () => null;

export default SetupModal;
